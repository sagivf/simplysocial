div(ng-class="{ 'ins-masonary': content.masonary === 'yes' }")
  article.ins-masonary-item.angular-animate(ng-repeat="post in content.posts", ng-class="{ 'ins-article-expanded': post.expanded }")
    .ins-actions
      i.icon-arrow-back-outline
      i.icon-heart
      span(am-time-ago="post.time")
    .ins-body
      img(ng-src="external/avatars/{{post.userImage}}")
      div.ins-details
        h3 {{post.name}}
        p(ng-bind-html='post.text')
        div.ins-expand(ng-if="post.comments.length")
          a(ng-click="post.expanded = !post.expanded")
            | {{ post.expanded ? 'Collapse' : 'Expand' }}
            i(ng-class="{ 'icon-triangle-up': post.expanded, 'icon-triangle-down': !post.expanded }")
    div(ng-show="post.expanded").ins-comments
      ul
        li(ng-repeat="comment in post.comments")
          .ins-body
            img(ng-src="external/avatars/{{comment.userImage}}")
            div.ins-details
              h3 {{comment.name}}
              p(ng-bind-html='comment.text')
      .ins-respond
        input(type="text", placeholder="Reply...")
    img(ng-if="post.image", ng-src="external/posts/{{post.image}}")
